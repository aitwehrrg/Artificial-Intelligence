#include "../_Graph/graph.cpp"
#include <bits/stdc++.h>

using namespace std;

vector<Vertex*> bestFirstSearch(Graph* G, Vertex* src) {
    set<Vertex*> visited;
    priority_queue<Vertex*, vector<Vertex*>, compare> q;
    vector<Vertex*> result;

    return result;
}
